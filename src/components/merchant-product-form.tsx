"use client";

import { useState } from "react";
import { Edit3, Loader2, Plus, X } from "lucide-react";
import { useFormStatus } from "react-dom";

type ProductAction = (formData: FormData) => void | Promise<void>;
type Product = { id: string; name: string; description: string | null; image_url: string | null; price_usd: number; stock: number };

function SubmitButton({ editing }: { editing: boolean }) { const { pending } = useFormStatus(); return <button disabled={pending} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-emerald-600 px-5 text-sm font-semibold text-white transition hover:bg-emerald-700 disabled:opacity-70">{pending ? <Loader2 className="h-4 w-4 animate-spin" /> : editing ? <Edit3 className="h-4 w-4" /> : <Plus className="h-4 w-4" />}{pending ? "Guardando..." : editing ? "Guardar cambios" : "Crear producto"}</button>; }

export function MerchantProductForm({ storeId, action, product }: { storeId: string; action: ProductAction; product?: Product }) {
  const [open, setOpen] = useState(false);
  const editing = Boolean(product);
  return <><button onClick={() => setOpen(true)} className={editing ? "inline-flex h-9 items-center gap-2 rounded-lg px-3 text-xs font-semibold text-slate-500 transition hover:bg-emerald-50 hover:text-emerald-700" : "inline-flex h-11 items-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white shadow-sm transition hover:bg-emerald-700"}>{editing ? <Edit3 className="h-4 w-4" /> : <Plus className="h-4 w-4" />}{editing ? "Editar" : "Nuevo producto"}</button>{open ? <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4 backdrop-blur-sm" role="dialog" aria-modal="true"><div className="w-full max-w-lg rounded-2xl border border-slate-200 bg-white p-6 shadow-2xl dark:border-white/10 dark:bg-slate-900"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-emerald-600">Catálogo</p><h2 className="mt-1 text-xl font-bold">{editing ? "Editar producto" : "Nuevo producto"}</h2></div><button onClick={() => setOpen(false)} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X className="h-5 w-5" /></button></div><form action={action} className="mt-6 grid gap-4"><input type="hidden" name="store_id" value={storeId} />{product ? <input type="hidden" name="product_id" value={product.id} /> : null}<label className="grid gap-1.5 text-sm font-semibold">Nombre<input name="name" required maxLength={160} defaultValue={product?.name ?? ""} placeholder="Ej. Camisa de lino" className="h-11 rounded-xl border px-3 font-normal outline-none focus:border-emerald-500" /></label><label className="grid gap-1.5 text-sm font-semibold">Descripción<textarea name="description" maxLength={1000} defaultValue={product?.description ?? ""} placeholder="Descripción breve" className="min-h-24 rounded-xl border p-3 font-normal outline-none focus:border-emerald-500" /></label><label className="grid gap-1.5 text-sm font-semibold">Imagen (URL opcional)<input name="image_url" type="url" defaultValue={product?.image_url ?? ""} placeholder="https://..." className="h-11 rounded-xl border px-3 font-normal outline-none focus:border-emerald-500" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1.5 text-sm font-semibold">Precio USD<input name="price_usd" required type="number" min="0.01" step="0.01" defaultValue={product?.price_usd ?? ""} className="h-11 rounded-xl border px-3 font-normal outline-none focus:border-emerald-500" /></label><label className="grid gap-1.5 text-sm font-semibold">Stock<input name="stock" required type="number" min="0" step="1" defaultValue={product?.stock ?? 0} className="h-11 rounded-xl border px-3 font-normal outline-none focus:border-emerald-500" /></label></div><div className="mt-2 flex justify-end gap-3 border-t pt-5"><button type="button" onClick={() => setOpen(false)} className="h-11 rounded-xl border px-4 text-sm font-semibold text-slate-600">Cancelar</button><SubmitButton editing={editing} /></div></form></div></div> : null}</>;
}
